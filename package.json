{
  "name": "y-router",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:cloud-run": "gcloud run deploy y-router --source . --platform managed --region asia-southeast1 --allow-unauthenticated --cpu=1 --min-instances=0 --max-instances=1",
    "start": "tsx server.ts",
    "dev:local": "tsx --watch server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "tsx": "^4.6.0"
  }
}
